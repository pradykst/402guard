{
  "name": "@402guard/server",
  "version": "0.0.1",
  "main": "dist/index.cjs",
  "module": "dist/index.mjs",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsup src/index.ts --format cjs,esm --dts --clean",
    "demo": "DOTENV_CONFIG_PATH=../../.env tsx -r dotenv/config examples/express-demo.ts",
    "clean": "rimraf dist"

  },
  "dependencies": {
    "@402guard/subscriptions": "workspace:*",
    "express": "^4.19.2"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
